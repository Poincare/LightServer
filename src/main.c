#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/types.h>
#include <sys/socket.h>
#include <netdb.h>
#include <arpa/inet.h>

/* Parse the configuration file */
void parse_config(char *filename);

int main (int argc, char **argv) {
	printf("Starting LightServer...");
	
	return 0;
}
